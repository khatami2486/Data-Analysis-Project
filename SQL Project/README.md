# SQL Project
In simple terms, Data Analysis using SQL is the process of talking to a database to find answers. SQL is the language used to pull, clean, and calculate data stored in large systems. Instead of looking through thousands of rows manually, you write "queries" (commands) to get exactly what you need.
